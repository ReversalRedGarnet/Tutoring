/* ------------------------------------------------------------------
   config.js — the few things you will actually want to change.
   ------------------------------------------------------------------ */

window.CONFIG = {

  /* CHANGE THIS. The suggestion box opens a prefilled email to this
     address. Until it is a real address the button will not do
     anything useful.

     Keep in mind this sits in the repo in plain text, which is fine
     while the repo is private and a spam magnet if it ever is not. */
  tutorEmail: 'REPLACE-ME@example.com',

  /* How many unit cards show before "Show more". Elliot has six, so
     all of them fit — drop this to 4 if a learner ever has more. */
  unitsVisible: 6
};
